# ── Secrets ────────────────────────────────────────────────────────────────
# .env ตัวเดียวที่ราก ใช้ร่วมกันทั้ง 3 service (ห้าม commit ของจริงเด็ดขาด)
.env

# ── AI tooling ─────────────────────────────────────────────────────────────
.aider*

# ── Node / frontend ────────────────────────────────────────────────────────
# กันเคส `npm install` / `npx` หลุดไปรันที่รากแล้วโผล่มาเป็น untracked ทั้งกอง
node_modules/

# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/

# ── Build output ───────────────────────────────────────────────────────────
dist/
build/
